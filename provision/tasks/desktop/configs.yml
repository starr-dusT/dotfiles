- name: Remove pre-installed gnome software
  dnf:
    name:
      - rhytmbox
      - cheese
      - totem
      - gnome-maps
      - gnome-characters
      - gnome-connections
      - libreoffice
      - libreoffice-writer
      - libreoffice-impress
      - libreoffice-calc
      - rhythmbox
      - gnome-tour
      - yelp
    state: absent
  become: true